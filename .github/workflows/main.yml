name: Workflow Automation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  automate-task:
    runs-on: ubuntu-latest

    env:
      

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Echo the API Key
      run: echo "API_KEY is: ${app_d.APP}"
